export type Animal = {
  id: string;
  name: string;
  commonName: string;
  description: string;
  image: string;
};

export type Feedback = {
  id: number;
  name: string;
  city: string;
  text: string;
  month: string;
  year: string;
};